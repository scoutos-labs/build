# agent-harness handoff

- 2026-07-27 begin — slug `agent-harness`; goal: replace single-shot JSON patch agent with a bounded tool-calling harness (web_search/web_fetch/web_post server-side, fs_*/exec on the WebContainer as control plane) + curated multi-model picker. Decisions locked with user: trust-the-sandbox approvals (gate only web_post + .env secrets), managed-only web tools, curated tiers + advanced OpenRouter list. Workspaces (agents/skills/workflow) designed in PRD, staged for follow-up loops. Reference: private repo twilson63/chat cloned to scratchpad/chat-ref.
- 2026-07-27 plan-approved — PRD `docs/agent-harness-prd.html` revised after plan gate returned NOT APPROVED / 12 blockers; all resolved in PRD §17. Restructured 13 steps → 12 units, release-1 line at U9. Key blocker fixes: U3 added for the missing tool transport (openrouter.ts:137 rejects content:null); Ollama cut from tool mode; wire shape pinned to tree+fullFiles; apply_patches retirement made explicit; undo must use webcontainer.RemountRequested (project.RemountProject discards files); timeout path is dead code (no update_agent branch); ResetProject bypasses cancel; npx narrowed to {tsc,vite}.
- 2026-07-27 U1/U2/U3/U5 done — server spine (models+limiter+step tokens), loop actor, tool transport, fs+exec tools. Live spike settled B3: free-tier qwen3.6-35b-a3b IS tool-capable. Remaining: U4 step endpoint, U6 prompt+parity, U7a/U7b wiring, U8 trail UI, U9 picker.
- 2026-07-27 checkpoint — U1–U6 complete, green, and DARK. Harness fully built; nothing dispatches the new actor messages, so app behavior is byte-identical to merge-base (update.gleam diff empty, smokes 25/25 + 14/14). Next: U7a wires the loop + retires apply_patches/InstallIfNeeded(patches) + converts improve_selected_element; U7b fixes the dead AgentTimeoutReached path and the ResetProject cancel bypass; U8 trail UI; U9 picker. Tests: gleam 164, vitest 212, server 198 (merge-base 140/108/103).
- 2026-07-27 complete (release 1) — U1–U9 shipped: tool-calling loop live, patch protocol removed, activity trail, job picker. gleam 171 / vitest 215 / server 198; smokes 25/25 + 14/14 + 22/22 (new smoke-agent-harness.mjs). Remaining: U10 undo, U11 server web tools (web_search/web_fetch/web_post), U12 fs_delete. Also open: no live-model run yet; BYOK-OpenRouter still on the single-shot JSON path (client step transport exists and is tested, runStep only loops in managed mode).
- 2026-07-27 U11 + judge — web tools shipped (url-guard/safe-http/injection-guard/web-search ports, web_post approval card, tainted-turn rule enforced in 3 places). Implementation judge found 3 real blockers (orphaned tool result after an approved POST; server trail rows dropped; BYOK lost chips/story-paths/install) — all fixed with regression tests. gleam 180 / vitest 220 / server 305; smokes 25-25 + 14-14 + 29-29. Taste 4/5, originality 4/5. Remaining: U10 undo, U12 fs_delete, live-model run.
- 2026-07-27 all twelve units complete — U10 undo + U12 fs_delete landed; judge advisories A1 (concurrent runStep race) and A2 (server web reads lost between steps) closed. gleam 191 / vitest 225 / server 307; smokes 25-25 + 14-14 + 37-37. Remaining: live-model run; BYOK-OpenRouter still single-shot JSON (client step transport exists and is tested, runStep only loops in managed mode).
- 2026-07-27 BYOK tool mode — BYOK-OpenRouter now runs the same loop (no web tools; Ollama stays single-shot JSON). Client tool-mode prompt added as a parity-guarded mirror. Two bugs surfaced: FetchLLMClient held globalThis.fetch unbound (pre-existing, would break BYOK JSON in prod), and the transcript window was one step wide in BOTH modes (12 steps for 2 steps of work). gleam 191 / vitest 234 / server 307; smokes 25-25 + 14-14 + 37-37. Only a live-model run remains.
