# Copy to .env.local for local development (gitignored).
# Managed auth (Clerk sign-in gate + server-proxied agent calls).
VITE_MANAGED_AUTH=true
# Clerk dashboard → API keys → Publishable key
VITE_CLERK_PUBLISHABLE_KEY=
